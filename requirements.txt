# ─── Core ─────────────────────────────────────────────────────────────────────
youtube-transcript-api>=0.6.0
aiohttp>=3.9.0
aiofiles>=23.2.0        # async file I/O used in pipeline scripts
anthropic>=0.40.0
openai>=1.0.0           # for vLLM API compatibility (OpenAI-compatible endpoint)
tqdm>=4.66.0
python-dotenv>=1.0.0
requests>=2.31.0
httpx>=0.27.0
pydantic>=2.0.0         # api_server.py uses Pydantic v2 validators
typer>=0.12.0           # CLI entrypoints in discovery/synthesis scripts
loguru>=0.7.0           # Structured logging throughout pipeline
datasketch>=1.6.5       # MinHash deduplication in validation
rich>=13.7.0            # Rich terminal output in pipeline scripts

# ─── 3D Data ──────────────────────────────────────────────────────────────────
trimesh>=4.0.0
objaverse>=0.1.0
huggingface_hub>=0.20.0
Pillow>=10.0.0
numpy>=1.26.0
scipy>=1.12.0

# ─── Optional 3D formats (install what's available) ───────────────────────────
rhino3dm>=8.0.0
ezdxf>=1.1.0
# USD: pip install usd-core  (large ~500MB, install separately if needed)

# ─── YouTube / Google ─────────────────────────────────────────────────────────
google-api-python-client>=2.100.0
google-auth>=2.22.0

# ─── Training (GPU server only) ───────────────────────────────────────────────
torch>=2.1.0
transformers>=4.37.0
datasets>=2.16.0
peft>=0.8.0
trl>=0.7.0
deepspeed>=0.13.0
accelerate>=0.26.0
bitsandbytes>=0.42.0
# flash-attn>=2.5.0  # install separately: pip install flash-attn --no-build-isolation
wandb>=0.16.0
sentencepiece>=0.1.99

# ─── vLLM (GPU server only) ───────────────────────────────────────────────────
# vllm>=0.3.0  # install separately: pip install vllm (only on GPU machines)

# ─── Deploy ───────────────────────────────────────────────────────────────────
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
redis>=5.0.0
websockets>=12.0

# ─── Audio (voice input in plugins) ──────────────────────────────────────────
sounddevice>=0.4.6
openai-whisper>=20231117

# ─── Evaluation ───────────────────────────────────────────────────────────────
scikit-learn>=1.4.0
rouge-score>=0.1.2

# ─── Scraping ─────────────────────────────────────────────────────────────────
beautifulsoup4>=4.12.0
lxml>=5.0.0
